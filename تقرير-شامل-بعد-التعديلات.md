# تقرير شامل — AETHER-STUDIO / AURA Studio (بعد التعديلات)

**التاريخ:** 2026-08-01 | **الفرع:** `arena/019fbbfe-aether-studio` | **الكوميت:** `6d21b26` — "Fix: AURA nav bar overlapping video — unified top bar" | **الحالة:** Working tree clean (متطابق مع `origin/main`)

> هذا التقرير هو مراجعة كاملة للوضع الحالي، ويُقرأ بجانب التقرير السابق `تقرير-شامل-قبل-التعديلات.md` (2026-07-31) لملاحظة التقدّم الذي حصل.

---

## 1. نظرة عامة سريعة

**AURA Studio** — منصة توثيق مشاريع البناء كـ**فيلم متواصل واحد**:
> من أول حجر… حتى آخر غرفة — One continuous living record

- **Canvas سينمائي** يعرض 97 إطاراً متتابعاً لبناء الفيلا (حفر ← أساسات ← هيكل ← بناء ← واجهات ← إنجاز ← ليلي)
- **8 ويدجات** زجاجية فوق الفيديو (توقفات عند بداية كل مرحلة) + **4 أقسام بيضاء** تحت الفيديو
- **لوحة تحكم خفية** (3 نقرات على الشعار / Ctrl+Shift+A / المسار المباشر + كلمة مرور)
- **ثيم فاتح** موحد (`#FAFAF7`) بلمسات ذهبية (`#BFA76A`) — مع تبديل عربي/إنجليزي

**الأرقام:** 552 ملف، ~21.8MB محتوى (بدون `.git`)، 45MB إجمالي مع `.git`، أصول `assets/` = **23MB** (كانت ~54MB).

---

## 2. حالة المستودع

| البند | القيمة |
|---|---|
| الفرع الحالي | `arena/019fbbfe-aether-studio` |
| الكوميت الوحيد في التاريخ | `6d21b26` (تاريخ مختصر — squash) |
| علاقته بـ `origin/main` | متطابق تماماً (نفس الـ hash) |
| حالة الشجرة | نظيفة (لا تعديلات غير محفوظة) |
| آخر تعديل | توحيد الشريط العلوي (top bar) لمنع تداخله مع الفيديو |

---

## 3. هيكل الملفات (فعلي)

```
AETHER-STUDIO/
├── index.html            (1931 سطر، 111KB) — الموقع الرئيسي: hero سينمائي + ويدجات + 4 أقسام
├── admin.html            (2826 سطر، 146KB) — لوحة التحكم الكاملة
├── effects-demo.html     (228 سطر) — معرض تجريبي لتأثيرات البناء (Ken Burns / Mask Reveal…)
├── js/
│   ├── cms.js            (642 سطر، 44KB) — Data model + store + i18n
│   ├── supabase.js       (263 سطر، 10KB) — Backend client (DB + Storage + fallback)
│   └── build-effects.js  (390 سطر، 13KB) — كلاس BuildEffects (لم يُدمج بعد في الموقع)
├── assets/               (23MB — 100% webp)
│   ├── seq/              (97 إطار رئيسي + 97 thumb، 1408×768، ~30-60KB لكل إطار) = 5.2MB
│   ├── stages/           (6 بوسترات s1–s5 + s7) = 312KB
│   ├── gallery/          (7 مراحل × 8 صور × نسختين full+thumb = 112 ملف) = 3.0MB
│   ├── rooms/            (16 صورة غرف + 4 pano 2816×512 + 109 إطار seq + 96 صورة معرض) = 14MB
│   └── villa-*.webp      (5 صور رئيسية) ≈ 400KB
├── supabase-schema.sql   (162 سطر) — نسخة v2 محسّنة وآمنة (RLS مقفول)
├── vercel.json           — cleanUrls + cache headers + rewrite /admin
├── README.md             (159 سطر)
├── LICENSE               — Proprietary
├── AURA-STUDY.md         (451 سطر) — دراسة المشاكل والحلول
├── AURA-STUDIO-PROMPT.md (200 سطر) — البرومبت الكامل / خارطة الطريق v1→v15.1
├── تقرير-شامل-قبل-التعديلات.md  — التقرير السابق (خط الأساس)
└── .gitignore
```

**ملاحظة توثيقية:** README يذكر «لا توجد ملفات برومبت» وبنية بدون `supabase.js` و`effects-demo.html` والملفات التوثيقية — البنية الفعلية أوسع من الموصوفة في README.

---

## 4. ما الذي تغيّر منذ التقرير السابق؟ (خلاصة التقدّم)

| البند | قبل (2026-07-31) | الآن (2026-08-01) | الحالة |
|---|---|---|---|
| الثيم | داكن `#0E0E10` | فاتح موحد `#FAFAF7` + ذهبي | ✅ |
| الشريط العلوي | شريطان متداخلان | شريط موحد (nav + مشاريع) يختبئ عند التمرير | ✅ (آخر كوميت) |
| صور rooms | 39MB PNG ضخمة | 14MB WebP (كل شيء webp، صفر PNG) | ✅ |
| صور stages | 7.5MB PNG | 312KB WebP | ✅ |
| RLS في Supabase | مفتوح للجميع (public write) | v2 آمن: قراءة عامة، كتابة للمصادقين فقط | ✅ (لكن انظر §8) |
| تكرار دوال admin | 6 دوال مكررة حرفياً | دالة واحدة لكل منها | ✅ |
| XSS | innerHTML بدون escape | `esc()/escAttr()/safeUrl()` في index + `escapeHtml()` في admin | ✅ جزئياً |
| محرر الإطارات (frames) | `buildFrames()` ميت (استدعاءات بلا تعريف) | موجود ويعمل (رفع/استبدال/حذف/سحب للترتيب) | ✅ |
| المزامنة | storage + polling فقط | + **BroadcastChannel** (نفس التبويب) + polling 5s | ✅ |
| محرر تسلسل الويدجات | غير موجود | موجود في admin (رفع + autoplay + interval) | ✅ |
| SEO & Analytics | غير موجود | قسم جديد في admin (title/desc/og/GA/canonical) | ✅ |
| System Health | غير موجود | لوحة صحة النظام (عدد الصور، حجم localStorage) | ✅ |
| تصدير/استيراد | غير موجود | Export/Import config كامل | ✅ |
| تأثيرات البناء | غير موجودة | `js/build-effects.js` + `effects-demo.html` (تجريبية، غير مدمجة) | 🟡 |

**التقدير العام: تم تنفيذ ~90% من خارطة الطريق حتى v15.1.**

---

## 5. تحليل `index.html` (الموقع الرئيسي)

### الهيكل
- **Top bar موحد:** شعار (بوابة الأدمن) + 4 روابط + تبديل لغة + CTA + شريط مشاريع (Villa Solara / Villa Nairo)
- **Hero:** canvas `#cine` + حلقة تقدم SVG + HUD زجاجي للمراحل (7 كروت) + أزرار تجارب (360°/جولة حرة/قبل-بعد) + overlay widget + playbar + Build Timeline أفقي + زر «جولة كاملة»
- **أقسام بيضاء:** خدماتنا (4 كروت → مودال) / من نحن (نصوص + إحصائيات) / عملاؤنا (marquee) / تواصل (فورم + واتساب + خريطة OpenStreetMap)
- **مودالات:** مودال أبيض للويدجت + Details View (صفحة تفصيلية كاملة داخل الموقع) + toast

### منطق التشغيل
```js
SEGS = [{type:'ext', count, src: n => AURA.frameSrc(PROJ,n)}]
TOTAL = count
loop(rAF): target += FPS*dt → current += (target-current)*0.14
  عند دخول مرحلة جديدة (from === g) → scheduleStagePause(): توقف + overlay widget + auto-resume بعد 5s
  draw(current): يبحث في cache عن أقرب إطار محمّل (fallback search)
```

### نقاط قوة
- حماية `esc/escAttr/safeUrl` مطبقة على كل الإخراج الديناميكي في المودالات والـ details view
- fallback قوي للصور: إطارات مخصصة ← إطارات المراحل ← المسار الافتراضي `assets/seq/`
- مؤشرات فورية: HUD يتحدث live داخل الحلقة، scrub + ticks + timeline ملون قابل للنقر
- تبديل مشروع سلس مع fade + إعادة بناء كاملة آمنة (try/catch حول كل خطوة)
- جولة كاملة (Full Tour) + روابط مباشرة `#villa-nairo` + جولة 360° بسحب + قبل/بعد بسلايدر + دعم YouTube/Vimeo embeds
- مزامنة ثلاثية: `storage` event + BroadcastChannel + polling 5 ثوانٍ (مع `_lastUpdate`)

### ملاحظات / مشاكل
1. **مشكلة i18n افتراضية:** `cms.js` يبدأ بـ `lang='en'` بينما HTML مكتوب عربي مع `dir="rtl"` وREADME يقول «العربية هي الافتراضي». الزائر الأول (بدون localStorage) يرى محتوى إنجليزي مع اتجاه RTL — تناقض يستحق الإصلاح (جعل الافتراضي `ar` أو القراءة من `document.documentElement.lang`).
2. `fitCanvas()` بلا debounce على كل resize.
3. `cache` غير محدود (كل الإطارات المحمّلة تبقى في الذاكرة) — مقبول مع 97 إطاراً صغيراً، لكنه خطر مع مشاريع كبيرة.
4. تسلسل الويدجات (`widgetSequences`) يعمل فقط كمؤقّت — لا يوجد عرض مرئي فعلي للصور داخل الـ overlay (التعليق في الكود يقول «for now, just update the overlay background subtly» — عملياً لا شيء يظهر).
5. `window._lastCfgUpdate` لا يُهيّأ مبدئياً → أول polling يشغّل `applyAllFromStorage()` كاملاً بلا داعٍ (تافه).
6. `openExp360/loadPano/openExpWalk/openExpBA` تبني innerHTML ببيانات (أسماء غرف، عناوين، embed URLs) **بدون** `esc()/safeUrl()` — خطر XSS متوسط إذا جاءت البيانات من مصدر ملوّث.
7. `applyAllFromStorage()` يعيد بناء DOM كامل عند كل تغيير — مقبول حالياً، لكنه سيكلف أكثر مع نمو المشاريع.

---

## 6. تحليل `admin.html` (لوحة التحكم — 2826 سطراً)

### الأقسام (9 صفحات)
1. **Projects Manager** — قائمة سحب وإفلات + إضافة/نسخ/حذف/تعيين افتراضي + تبويبات (Identity / Stages / Experiences)
2. **Frames Editor** — شبكة إطارات بالسحب للترتيب + رفع/استبدال/حذف + ترقيم تلقائي
3. **Widgets Editor** — كل ويدجت: عنوان/وصف عربي+إنجليزي + معرض + نقاط + **تسلسل صور** (جديد)
4. **Before/After Editor** — أزواج صور + معاينة
5. **Hotspots Editor** — 4 أوضاع (غرف/فيديو/قبل-بعد/معارض) بنقاط مئوية x,y + سحب + تحرير
6. **Site Settings** — بيانات التواصل + نصوص hero + من نحن + العملاء (رفع شعارات) + **SEO & Analytics** (جديد)
7. **Inquiries** — Supabase أولاً ثم localStorage، فلترة بالحالة، تحديث حالة، حذف، اتصال، واتساب، تصدير CSV
8. **Playback** — معاينة حية حقيقية (canvas + playhead + stage markers + FPS + مدة الفيلم)
9. **UI** — اسم العلامة، اللون، شفافية الزجاج، blur، radius، glow + System Health (جديد) + تصدير/استيراد config

### ملاحظات إيجابية
- التكرار القديم أُزيل نهائياً (كل دالة معرفة مرة واحدة)
- `boot()` يحمي كل builder بـ try/catch حتى لا يُسقط فشل قسمٍ الباقي
- `$()` يُستخدم للعناصر (رغم بقاء بعض الاعتماد على globals بمعرّفات الـ id)
- الحفظ يدفع لـ Supabase أينما أمكن (`saveProject/saveSiteContent/saveHotspots`)
- تحذير قبل `resetAll()` بنقرتين تأكيد

### مشاكل
1. **كلمة المرور ثابتة ومكشوفة:** `aura2026` في الكود + **معروضة كـ hint في شاشة الدخول نفسها** — أي شخص يصل `/admin.html` يعرفها فوراً. لا hash، لا حساب حقيقي (فقط `email` نصي + role من بادئة `admin`).
2. **XSS في حقول admin:** قوالب `innerHTML` تُدخل قيماً خام في `value="..."` (عناوين الويدجات، النقاط، أسماء العملاء، أسماء النقاط الساخنة) بدون `escapeHtml()` — escape موجود لكنه مستخدم في inquiries فقط.
3. `buildInquiries()` يقرأ `inq.date/project/widget` بينما صف Supabase يحوي `created_at/project_type/source` → الطلبات القادمة من Supabase تظهر بتاريخ «Invalid Date» وproject=— وwidget=— (خلل عرض بسبب عدم تطابق الـ schema).
4. `clearInquiries()` يمسح المحلي فقط — لا يمسح Supabase.
5. الملف ضخم (2826 سطراً) — صعوبة صيانة، لا modules.

---

## 7. تحليل `js/cms.js` — قلب البيانات

- **مشروعان افتراضيان:** `VILLA_SOLARA` (كامل: 7 مراحل + pausePoints + 8 غرف + أثاث + 12 ويدجت + قبل/بعد + تجارب + hotspots + widgetSequences) و`VILLA_NAIRO` (مبسّط، 65%، قيد التنفيذ)
- **Store:** `AURA.{load,save,reset,getProjects,getCurrent,setCurrent,addProject,deleteProject,updateProject,reorder,frameSrc,getAssembledImages,applyTheme}`
- **`_deepMerge`:** دمج عميق بين الافتراضي والمحفوظ → إضافة حقول جديدة لا تكسر البيانات القديمة (schema migration مجاني)
- **`getAssembledImages()`:** تسلسل مخصص ← تجميع من المراحل ← مسار افتراضي
- **`AURA_I18N`:** تبديل AR/EN + `splitPair("نص · Text")` + MutationObserver للمحتوى الديناميكي

**ملاحظات:**
- `lang` الافتراضي `'en'` (انظر §5-1)
- `frameSrc` لا يتحقق من وجود الملف فعلياً — يعتمد على `onerror` عند التحميل
- furniture/hotspots بلا validation لنطاق 0-100%

---

## 8. تحليل `js/supabase.js` + `supabase-schema.sql` — الخلفية والأمان

### الإعداد
- `SUPABASE_URL` = مشروع `ykhlvghqrnpwkipaowem` + **publishable key** (مكشوف في الكود — متوقع لاستخدام client-side)
- تحميل SDK من CDN ديناميكياً + `window.sb`
- `AURA_DB`: loadAll/saveAll/saveProject/saveSiteContent + inquiries (CRUD) + `uploadMedia` (Storage أولاً ← ضغط Base64 JPEG ≤1600px كبديل) + saveHotspots

### schema v2 — تحسّن كبير
| الجدول | القراءة | الكتابة |
|---|---|---|
| projects | عام | **مصادق فقط** |
| site_content | عام | **مصادق فقط** |
| inquiries | **مصادق فقط** | INSERT عام (نموذج التواصل) + تعديل/حذف مصادق |
| images | عام | **مصادق فقط** |
| storage.objects | عام | **مصادق فقط** |

+ Indexes (`sort_order`, `is_default`, `status`, `created_at`, `project_id`) + default site_content.

### ⚠️ أهم ملاحظة أمنية/تشغيلية حالياً
- السياسات الجديدة تتطلب `auth.role() = 'authenticated'`، **لكن لا يوجد أي تسجيل دخول Supabase في admin** (لا `signInWithPassword`) — أي كتابة من الأدمن ستُرفض الآن (الـ key publishable يعمل كـ anon) → `saveProject/saveSiteContent/saveHotspots` ستفشل وتسقط على localStorage فقط.
- **النتيجة:** القفل الأمني صحيح، لكنه كسَر المزامنة الفعلية مع Supabase حتى يُضاف Auth. الخياران: (أ) إضافة `signInWithPassword` عند دخول الأدمن وربط السياسات به، أو (ب) سياسة DEMO مؤقتة للكتابة العامة (موجودة كتعليق في الـ SQL).
- الحماية مثالية للـ demo الحالي (لا حساسية)، لكن لا يجوز الإنتاج بهذه الحالة.
- `uploadMedia` بديل Base64 قد يتجاوز حد localStorage (~5MB) عند رفع عشرات الصور.
- **لا Rate Limiting** على نموذج التواصل (أي بوت يستطيع ملء الجدول بطلبات).

---

## 9. الأصول والصور (Assets)

| المجلد | الحجم | التفاصيل |
|---|---|---|
| `seq/` | 5.2MB | 97 إطار + 97 thumb (1408×768 webp) |
| `gallery/` | 3.0MB | 112 ملف (56 full + 56 thumb `-t`) — تُستخدم في admin و pausePoints |
| `rooms/` | 14MB | 16 صورة غرف + 4 pano (2816×512) + 109 إطار seq للغرف + 96 معرض — تُستخدم في hotspot editor |
| `stages/` | 312KB | 6 بوسترات (s1–s5, s7) — s6 غير موجودة (مقصود/شكلي) |
| جذر assets | ~400KB | villa, villa-clean, villa-blueprint, villa-wireframe, blueprint-alpha |

**إيجابي:** تحويل كامل لـ webp (صفر PNG) → انخفض حجم الأصول من ~54MB إلى 23MB. أكبر ملف الآن 143KB (pano-living) — ممتاز.
**ملاحظة:** `assets/rooms/seq/*` (109 إطار) تُستخدم حالياً فقط في محرر النقاط، وليس في تجربة الزائر — فرصة مستقبلية (جولات غرف متتابعة على الموقع).

---

## 10. المزامنة (Sync) — الوضع الحالي

- **`storage` event** بين التبويبات + **BroadcastChannel** `aura-cms` لنفس التبويب (جديد) + **polling 5 ثوانٍ** كحزام أمان
- عند أي تغيير: `applyAllFromStorage()` → إعادة CFG/PROJ/theme/playback/siteContent/projectBar/playbar/widgets + إعادة بناء الـ timeline إذا تغيّر عدد الإطارات أو الصور
- سرعة الفيديو (`fpsExt/pauseExplore`) أصبحت reactive عبر `applyPlayback()`

**المتبقي من الدراسة:** Supabase realtime subscriptions لم تُطبّق (الاعتماد كله على localStorage/BroadcastChannel) — كافٍ لنشر single-user، غير كافٍ لمزامنة متعددة الأجهزة.

---

## 11. الأمان — ملخص

| المخاطر | المستوى | الحالة |
|---|---|---|
| كلمة مرور الأدمن ثابتة + معروضة كـ hint على الشاشة | 🔴 مرتفع | أي زائر يعرفها؛ يجب hash + حساب حقيقي قبل الإنتاج |
| المصادقة client-side فقط (sessionStorage) — قابلة للتجاوز | 🔴 مرتفع | القفل فقط واجهة؛ الحماية الفعلية غائبة |
| Supabase: write الآن مقفول على authenticated لكن الأدمن لا يتسجّل → التزامن متوقف | 🟠 متوسط | يجب ربط auth بالأدمن أو سياسة demo مؤقتة |
| XSS: escape مطبق على الإخراج في index، لكن إدخالات admin الخام + innerHTML في تجارب 360°/walkthrough بدون esc | 🟠 متوسط | حقول admin + openExp* بحاجة escape |
| Rate limiting على نموذج التواصل | 🟠 متوسط | غير موجود |
| RLS، Storage، inquiries | 🟢 جيد | schema v2 محكم (بعد إضافة auth) |

---

## 12. الأداء

- **وزن الصفحة:** index.html 111KB + admin 146KB (كل CSS/JS inline) — لا splitting، لا minification
- **الصور:** webp صغيرة، cache-control `immutable` سنة للأصول في Vercel + `no-cache` للـ HTML/JS
- **تحميل الإطارات:** تسلسلي (25ms بينها) + preload radius 10 + lazy loading للصور الثانوية
- **لا Service Worker / PWA** — أول تحميل يجلب 5.2MB من إطارات الفيديو تدريجياً (مقبول محلياً)
- تقدير Lighthouse: Performance ~75-85 (بعد ضغط الصور)، Best Practices ~80 (بقايا innerHTML/globals)، SEO ~80 (meta موجودة لكن لا structured data، والقسم SEO في admin لا يُطبَّق على الـ HTML فعلياً — حقول فقط)

**ملاحظة:** حقول SEO في admin (title/desc/og/gaId/canonical) تُحفظ في البيانات لكن `index.html` **لا يقرؤها** — لا يوجد تطبيق فعلي لها على الوسوم بعد.

---

## 13. المشاكل المفتوحة (ملخص منظم بالأولوية)

### 🔴 حرج قبل الإنتاج
1. **أمان الأدمن:** كلمة مرور ثابتة + hint على الشاشة + لا auth حقيقي
2. **Supabase auth مفقود:** القفل الجديد يمنع كتابة الأدمن → لا مزامنة خلفية؛ أضف `signInWithPassword` أو فعّل سياسة demo
3. **XSS في admin/تجارب:** تعميم `escapeHtml` على كل الإدخالات + `esc/safeUrl` في `openExp*`

### 🟡 مهم
4. تطبيق إعدادات SEO من admin على الوسوم الفعلية في index.html
5. إصلاح عدم تطابق inquiries (date/project/widget vs created_at/project_type/source)
6. جعل اللغة الافتراضية متسقة (ar) + إصلاح `dir` عند en
7. عرض مرئي فعلي لتسلسل الويدجات في الـ overlay (البنية جاهزة)
8. Rate limiting / honeypot على نموذج التواصل
9. `clearInquiries()` يجب أن يمسح Supabase أيضاً

### 🟢 تحسينات
10. debounce على resize + حد أقصى لذاكرة الـ cache
11. تقسيم admin.html إلى modules/مكتبات (أو على الأقل minify)
12. دمج `build-effects.js` في الموقع الرئيسي (Ken Burns على البوسترات/الغرف)
13. استخدام `assets/rooms/seq/` في جولة الغرف على الموقع
14. تحديث README (بنية فعلية + ملفات البرومبت + supabase.js + effects-demo)
15. إضافة tests (ولو smoke tests) + lint

---

## 14. التوثيق

| الملف | التقييم |
|---|---|
| `README.md` | جيد جداً (concept + تشغيل + بنية + ويدجات + أدمن + تقنيات + تصميم) — لكن البنية المذكورة أقدم من الفعلي |
| `AURA-STUDY.md` | دراسة ممتازة للمشاكل — معظم بنودها نُفّذت (sync، عملاء، hotspots، pausePoints، تسلسل، Supabase) |
| `AURA-STUDIO-PROMPT.md` | خارطة طريق v1→v15.1 كاملة — مرجع ذهبي لإعادة البناء |
| `تقرير-شامل-قبل-التعديلات.md` | خط الأساس — هذا التقرير هو النسخة المحدثة |

---

## 15. التقييم العام

**AURA Studio اليوم مشروع ناضج ومبهر بصرياً:**
- ✅ فكرة المنتج واضحة ومميزة (فيلم بناء متواصل) ومنفذة بإتقان (canvas + HUD + توقفات + مودالات)
- ✅ كل الأصول webp مضغوطة (23MB بدل 54MB)
- ✅ XSS مُعالج جزئياً، RLS مؤمّن في الـ schema، sync أسرع بثلاث قنوات
- ✅ أدمن شامل يغطي كل احتياجات المحتوى (مشاريع/مراحل/إطارات/ويدجات/نقاط/طلبات/SEO)
- ✅ صفر اعتماديات، نشر فوري على أي host/Vercel

**العوائق الرئيسية قبل اعتباره «إنتاجياً»:** مصادقة الأدمن، إعادة تفعيل التزامن مع Supabase بعد قفل RLS، تعميم الـ escaping، وتطبيق SEO.

**جاهزية تقريبية: 85-90% من الرؤية (v15.1)** — الباقي أمان + تكاملات وليس ميزات جديدة.

---

*أُعد هذا التقرير تلقائياً بعد مراجعة كاملة للمستودع بتاريخ 2026-08-01.*

---

## 16. الإصلاحات المنفذة (2026-08-01 — بعد التقرير)

تم تنفيذ الإصلاحات التالية فعلياً على الكود (4 ملفات: `index.html`، `admin.html`، `js/cms.js`، `js/supabase.js` + تحديث `README.md`):

### 🔴 الأمان
1. **كلمة مرور الأدمن:** أُزيلت من شاشة الدخول (لم تعد hint معروضة) ومن README، ولم تعد موجودة كنص صريح في أي ملف. التحقق الآن عبر **SHA-256** (مع fallback FNV للمواقع غير HTTPS) — `crypto.subtle` يُقارن مع constant مجزّأ.
2. **قفل المحاولات:** بعد 5 محاولات فاشلة يُقفل الدخول 60 ثانية مع عدّاد ظاهر.
3. **البوابة الخفية:** 3 نقرات على الشعار/Ctrl+Shift+A لم تعد تخزّن كلمة المرور في sessionStorage — تضع flag فقط (`aura.gate.ok`).
4. **XSS:** تعميم `escAttr()`/`escapeHtml()`/`safeUrl()` على كل حقول الأدمن (ويدجات، نقاط، مراحل، عملاء، قبل/بعد، pausePoints، إطارات) + دوال التجارب في الموقع (`openExp360/loadPano/openExpWalk/openExpBA`).

### ⚠️ منع الحفظ التلقائي (كما طلبت)
5. **أُزيل الـ auto-save كل 30 ثانية نهائياً** — لا شيء يُنشر على الموقع إلا بضغطة «حفظ ونشر» الصريحة لكل قسم.
6. أُضيفت **شارة «● غير محفوظ»** ثابتة تظهر فور أي تعديل (input/change) وتختفي عند الحفظ، مع بقاء تحذير beforeunload.

### 🎬 تنظيم أوامر التشغيل
7. **صفحة Playback في الأدمن أعيد تنظيمها** إلى 3 أقسام مرقّمة: (1) التحكم الرئيسي — سرعة fps + مدة التوقف + تشغيل تلقائي + presets + زر حفظ بارز، (2) مدة الفيلم، (3) المعاينة الحية — وأصبح زر «جرّب على الموقع» ضمن المعاينة.
8. **في الموقع:** زر «جولة كاملة» انتقل من الزر العائم إلى **شريط التشغيل الموحّد** (restart | scrub | 🎬 tour | play)، مع **اختصارات لوحة مفاتيح**: مسافة = تشغيل/إيقاف، ←/→ = تنقّل سريع.

### 🎨 جودة العرض والترتيب
9. **اللغة الافتراضية أصبحت عربية** (`ar` + RTL) كما يفترض README والواجهة — مع مزامنة `dir` مع اللغة في الحالتين.
10. **SEO من الأدمن يُطبَّق فعلياً** على الموقع: title، description، OG image، canonical، و Google Analytics (كانت تُحفظ فقط بلا تأثير).
11. **تسلسل الويدجات أصبح مرئياً:** صورة مصغّرة داخل الـ overlay تتبدل تلقائياً أثناء التوقف (كانت تعمل كمؤقّت بلا عرض).
12. **أداء:** debounce على resize + **إخلاء ذاكرة الإطارات** البعيدة عن موضع التشغيل (cache bounded) + إزالة إعادة مزامنة أولى غير ضرورية.
13. إصلاح عرض الطلبات (inquiries) من Supabase: تطبيع `created_at/project_type/source` إلى `date/project/widget` — كان التاريخ «Invalid Date».
14. **مسح كل الطلبات** الآن يمسح Supabase أيضاً وليس localStorage فقط.
15. إصلاح تبديل لغات بطاقات الخدمات (كانت كلتا اللغتين تظهران معاً).
16. **ربط الأدمن بمصادقة Supabase:** عند نجاح الدخول يُحاول `signInWithPassword` بنفس البريد/كلمة المرور — بمجرد إنشاء مستخدم في Supabase Dashboard تعمل الكتابة للجداول/Storage مع السياسات المقفولة (بدون ذلك يبقى الوضع الحالي: localStorage fallback).

### ✅ الفحوصات
- `node --check` على كل الملفات (HTML inline JS + 3 ملفات JS) — سليم
- اختبارات منطقية لـ cms.js (تحميل/حفظ/دمج/frameSrc/i18n) — 6/6 ناجحة
- التحقق من هاش كلمة المرور (SHA-256 + FNV) — صحيح
- خادم محلي: كل الصفحات ترد 200

---

## 17. مراجعة تداخل الكروت ومواضعها في العرض (2026-08-01)

### المشاكل المكتشفة (قبل الإصلاح)
| # | العنصران المتداخلان | السبب |
|---|---|---|
| 1 | `glass-hud` (عمود المراحل، يمين أعلى) مع أزرار التجارب (360°/جولة/قبل-بعد) | كلاهما عند `right:24px` — عمود الـ HUD يمتد لأسفل مع تقدم الفيلم ويلتقي بالأزرار عند `top:220px` في وضع LTR |
| 2 | `glass-overall` (بطاقة الإنجاز، يسار أعلى) مع نص الـ hero | نص الـ hero بعرض 760px يتجاوز حدود البطاقة على الشاشات ≤1280px |
| 3 | عمود الـ HUD مع نص الـ hero | على 901–1008px يتقاطعان أفقياً |
| 4 | آخر بطاقات الـ HUD مع `buildTimeline`/`playbar` | العمود بارتفاع ~600px يلامس شريط التشغيل على الشاشات القصيرة (vh < ~820) |
| 5 | `buildTimeline` (bottom:78px) مع حافة الـ playbar العلوية | top حافة الـ playbar عند ~78px — التصاق |
| 6 | `.stage-cap` (bottom:88px) مع الـ timeline في وضع التابلت | تداخل 8px |
| 7 | `pause-badge` (أعلى المنتصف) مع eyebrow الـ hero | على الشاشات القصيرة (vh ≤ 820) |
| 8 | `#mobileProgress` (top:70) مع نص الـ hero في الجوال | كلاهما أعلى المنتصف |
| 9 | أزرار التجارب (top:112) مع نص الـ hero في الجوال | تجاور غير محسوب |

### الإصلاحات المطبقة (CSS في `index.html`)
1. **أزرار التجارب** انتقلت إلى **أسفل يسار** الشاشة على الديسكتوب (`bottom:128px;left:24px`) — بعيداً عن عمود الـ HUD (وفي RTL تُطوى إلى `right:24px` بعيداً عن الـ HUD المقلوب).
2. **نص الـ hero** على ≤1280px: `width:min(48vw,520px)` + `top:clamp(112px,16%,150px)` — لم يعد يلامس أي بطاقة جانبية.
3. **عمود الـ HUD مضغوط على ≤1280px** (حشوة أصغر + إخفاء إطارات المراحل) → ارتفاعه ~430px بدل ~600px — لا يصل أبداً إلى الـ timeline/playbar حتى على الشاشات القصيرة.
4. **`buildTimeline`** رُفع إلى `bottom:92px` (فجوة 14px فوق الـ playbar) وفي التابلت إلى 92px، و**`.stage-cap`** التابلت إلى `bottom:132px` — لا تداخل.
5. **`pause-badge` مخفي ≤900px** (حالة التشغيل ظاهرة في زر playbar) + **نص الـ hero ينزل إلى 90px** على الشاشات العريضة القصيرة (≤820px ارتفاعاً).
6. **التابلت (641–900px):** إخفاء البطاقتين الجانبيتين (`glass-overall` + `glass-hud`) والاعتماد على **`#mobileProgress`** المدمجة — هكذا يستوعب العرض نص الـ hero والأزرار.
7. **الجوال (≤640px):** ترتيب رأسي صريح: شارة التقدم (70px) ← نص الـ hero (110px) ← أزرار التجارب (230px) — صفر تداخل.
8. **حماية CSS إضافية:** `.widget-overlay.show + .stage-cap{opacity:0}` — كتأمين إضافي فوق ما يفعله JS (إخفاء تسمية المرحلة أثناء فتح كرت الويدجت).

### التحقق
- فحص هندسي برمجي لكل العناصر المتراكبة (مستطيلات: البطاقات، النص، الأزرار، overlay، stage-cap، timeline، playbar، الشارات) على 21 مقاس شاشة من 320×568 حتى 1920×1080 → **صفر تداخل فعلي** في كل المقاسات.
- التداخل الوحيد المتبقي هو `overlay ↔ stage-cap` وهو **مقصود ومُدار**: تسمية المرحلة تختفي تلقائياً (JS + CSS) أثناء ظهور كرت الويدجت.
- `node --check` على كل الملفات سليم، والصفحتان تردان 200.

---

## 18. جولة التحقق الشاملة (2026-08-01 — بعد المراجعات)

فحص برمجي شامل لكل شيء بعد كل الإصلاحات:

### ما تم فحصه وتأكيده ✅
1. **سلامة النحو:** `node --check` على `cms.js` / `supabase.js` / `build-effects.js` + JS المضمّن في `index.html` و`admin.html` — **كلها سليمة**.
2. **اختبارات منطقية:** cms.js (تحميل/حفظ/دمج/frameSrc/i18n عربي افتراضي) — 6/6 ناجحة.
3. **توازن الوسوم:** `div/section/header/footer/button/form/table/ul/a` في الصفحتين — **متوازن** (لا وسوم مكسورة).
4. **مراجع العناصر (88 في الأدمن، 63 في الموقع):** كل `$('id')` موجود في HTML ما عدا:
   - `buildTimeline` — يُنشأ ديناميكياً (مقصود).
   - 12 عنصراً في الأدمن تخص **أقساماً أُزيلت من الواجهة** (محرر الإطارات، الديكور، المعارض، المكتبة، heatmap) — **أُضيفت حراسة `if(!el)return`** لكل دوالها حتى لا ترمي أخطاء عند استدعائها، والمرجع الوحيد القابل للوصول (`assembleSequenceFromStages → buildFrames`) أصبح آمناً.
5. **معالجات onclick (65):** كل دالة مستدعاة موجودة — **صفر مراجع مفقودة**.
6. **تدقيق XSS آلي:** صفر إدراجات خام متبقية في `innerHTML` بعد إصلاحات هذه الجولة.
7. **أمان إضافي هذه الجولة:**
   - نصوص الـ hero والعملاء من إعدادات الأدمن أصبحت تُهرب (`esc/escAttr/safeUrl`) عند عرضها في الموقع.
   - روابط واتساب/بريد/هاتف تُنقّى من الأحرف الخطرة.
   - `Set Default` في إدارة المشاريع لم تعد تحقن `p.id` داخل معالج onclick نصي (أصبحت عبر dataset + listener).
8. **الأسرار والتنظيف:** لا وجود لـ `aura2026` / "Demo password" / "Auto-saved" / `tourBtn` في أي ملف.
9. **الخادم المحلي:** كل الصفحات ترد 200.
10. **التداخلات الهندسية:** 21/21 مقاس شاشة نظيف (من 320×568 إلى 1920×1080) — التداخل الوحيد المتبقي (overlay ↔ stage-cap) مُدار بـ JS/CSS مقصوداً.

### النتيجة
المشروع في حالة تشغيل سليمة: موقع سينمائي بدون تداخلات، أدمن آمن بدون حفظ تلقائي، كل الدوال الميتة محمية، وكل إدخالات المستخدم مُهرَّبة.

---

## 19. مراجعة سرعة الفيديو + زر الدمج + المشروع المحدد + محرر النقاط (2026-08-01)

بناءً على ملاحظات المستخدم (مع صور مرفقة) — تم إصلاح 4 محاور:

### 1) سرعة الفيديو والمعاينة (كانت محل شك)
- **المشكلة الحقيقية:** معاينة التشغيل في الأدمن كانت تعرض «لا توجد صور» و0 إطارات للمشروع الافتراضي (97 إطاراً في `assets/seq/`)، لأن `pbLoadFrames` كان يستخدم الصور المخصصة فقط ولا يرجع للإطارات الافتراضية → كل حسابات المدة والسرعة تبدو معطّلة.
- **الإصلاح:** إذا لم توجد صور مخصصة، تولّد المعاينة مسارات الإطارات الافتراضية (`frameSrc`) — المعاينة تشتغل فوراً، ومدة الفيلم تُحسب بشكل صحيح (عدد الإطارات/السرعة + زمن التوقفات)، مع شارة «يعرض الإطارات الافتراضية» وإظهار اسم المشروع الحالي وعدد الإطارات في صفحة أوامر التشغيل.
- **خلل ثانٍ في الموقع:** عند السرعات العالية كان الفيلم قد يقفز فوق إطار بداية المرحلة فيفوت التوقف. أُصلح الاكتشاف ليتم عبر **عبور الإطار** (`from > prevG && from <= g`) فيعمل في أي سرعة.
- أُضيفت فقرة «④ المشروع الحالي» في لوحة التحكم الرئيسية مع عدد الإطارات.

### 2) زر «دمج الصور في الفيديو» (كان مربكاً)
- استُبدل صفّ الأزرار الخافت بلوحة واضحة تحت المراحل: «🎞 تسلسل الفيديو» تعرض الحالة (افتراضي 97 / مخصص N صورة)، شرحاً بسيطاً ثنائي اللغة، وزراً واحداً كبيراً «🧩 دمج صور المراحل في الفيديو».
- الزر **يُعطَّل تلقائياً** مع تلميح «ارفع صوراً لمرحلة أولاً» عندما لا توجد صور، وتعرض اللوحة إحصاء «مراحل فيها صور: x/y — إجمالي: n».

### 3) إظهار المشروع المحدد (الشاشة الثانية)
- **المشروع الافتراضي أصبح مثبتاً أول القائمة** في إدارة المشاريع (📌 + شارة DEFAULT) — لأن هذا ما يظهر على الموقع.
- **الصف الجاري تحريره يتميّز** بإطار ذهبي ونقطة ●.
- عنوان الصفحة يوضح: «المشروع الافتراضي (يظهر على الموقع): …» ويتحدث تلقائياً.

### 4) محرر النقاط (Hotspots)
- **أُضيفت الحقول الناقصة** لتعديل كامل: اللون، الأبعاد، المورد، التشطيب (كانت تُحفظ في البيانات لكن لا توجد حقول تحرير لها).
- **حماية من الأعطال:** لا ينهار عند غياب `interior/rooms/furniture`، يختار أول غرفة تفاعلية تلقائياً، وصورة احتياطية عند فشل تحميل الصورة + ارتفاع أدنى للمسرح حتى تعمل النقرات دائماً.
- أُضيف زر **«حذف كل النقاط»** مع تأكيد، وتحسّنت نصوص الإرشاد (انقر ← عدّل ← تطبيق ← حفظ).

### ✅ الفحوصات
- `node --check` على كل الملفات سليم؛ كل العناصر الجديدة موجودة في HTML؛ معالجات onclick كلها معرفة؛ اختبارات cms.js ناجحة؛ الصفحتان تردان 200.
